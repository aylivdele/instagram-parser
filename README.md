# Instagram Trend Monitor - Telegram Mini App

Автоматический мониторинг Instagram конкурентов с детекцией вирусного контента на ранней стадии.

## 🎯 Ключевые возможности

- **Раннее обнаружение трендов**: Детекция постов, которые только начинают набирать популярность
- **Умный алгоритм**: Анализирует скорость роста просмотров, а не общее количество
- **Telegram уведомления**: Мгновенные алерты о вирусном контенте
- **Удобный интерфейс**: Telegram Mini App для управления мониторингом

## 🧠 Как работает алгоритм детекции

Система не ждет, пока пост наберет миллионы просмотров. Вместо этого она:

1. **Отслеживает скорость роста** - сравнивает текущую скорость (просмотры/час) со средней для аккаунта
2. **Детектирует аномалии** - если скорость роста превышает норму на 150%+, это тренд
3. **Фокус на свежем контенте** - анализирует только посты младше 48 часов
4. **Минимум ложных срабатываний** - требует подтверждения тренда минимум 2 проверками

**Пример:**
- Обычная скорость: 1000 просм/час
- Текущая скорость: 3500 просм/час
- Рост: +250% → 🚀 Алерт!

## 📋 Требования

- Python 3.8+
- Telegram Bot Token (от @BotFather)
- Instagram API доступ (опционально)
- Сервер для хостинга (VPS, Railway, Render и т.д.)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Создание Telegram бота

1. Откройте @BotFather в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Сохраните полученный токен

### 3. Создание Mini App

1. В @BotFather отправьте `/newapp`
2. Выберите своего бота
3. Введите название приложения
4. Загрузите `instagram-monitor.html` на любой хостинг:
   - GitHub Pages
   - Vercel
   - Netlify
   - Cloudflare Pages
5. Укажите URL в @BotFather

### 4. Настройка бэкенда

Отредактируйте файлы:

**api_server.py:**
```python
TELEGRAM_BOT_TOKEN = "ваш_токен_от_BotFather"
```

**instagram_monitor.py:**
```python
TELEGRAM_CHAT_ID = "ваш_telegram_id"  # Получите у @userinfobot
```

**instagram-monitor.html:**
```javascript
const API_URL = 'https://ваш-сервер.com/api';
```

### 5. Запуск

```bash
# Запуск API сервера
python api_server.py

# Или запуск только мониторинга без API
python instagram_monitor.py
```

## 🔧 Настройка Instagram API

### Вариант 1: Instagram Graph API (официальный)

**Плюсы:** Надежный, легальный
**Минусы:** Требует Business аккаунт, ограниченный доступ

1. Создайте Meta Developer аккаунт
2. Настройте Instagram Business аккаунт
3. Получите Access Token
4. Замените функцию `fetch_instagram_posts()` в `instagram_monitor.py`

### Вариант 2: Сторонние API (рекомендуется)

**Apify Instagram Scraper:**
```python
async def fetch_instagram_posts(self, username: str, limit: int = 10):
    url = "https://api.apify.com/v2/acts/apify~instagram-scraper/runs"
    
    payload = {
        "directUrls": [f"https://instagram.com/{username}"],
        "resultsType": "posts",
        "resultsLimit": limit
    }
    
    headers = {
        "Authorization": f"Bearer {APIFY_TOKEN}"
    }
    
    async with aiohttp.ClientSession() as session:
        async with session.post(url, json=payload, headers=headers) as resp:
            data = await resp.json()
            # Обработка ответа...
```

**RapidAPI Instagram API:**
```python
url = "https://instagram-scraper-api2.p.rapidapi.com/v1/posts"
querystring = {"username_or_id_or_url": username}
headers = {
    "X-RapidAPI-Key": "ваш_ключ",
    "X-RapidAPI-Host": "instagram-scraper-api2.p.rapidapi.com"
}
```

### Вариант 3: Веб-скрапинг (не рекомендуется)

⚠️ Может нарушать Terms of Service Instagram

## 📊 Структура базы данных

SQLite база данных с 3 таблицами:

**competitors** - список отслеживаемых аккаунтов
**post_snapshots** - история метрик постов (для расчета скорости)
**alerts** - обнаруженные трендовые посты

## 🎛️ Настройка чувствительности

В `instagram_monitor.py` можно настроить параметры детекции:

```python
# Порог детекции тренда (процент роста от нормы)
is_trending = (
    growth_rate > 150 and  # Изменить на 100, 200 и т.д.
    current_hours < 24 and  # Возраст поста в часах
    len(snapshots) >= 2  # Минимум проверок для подтверждения
)
```

## 🚀 Деплой на продакшн

### Railway

```bash
railway login
railway init
railway up
```

### Render

1. Подключите GitHub репозиторий
2. Выберите "Web Service"
3. Команда запуска: `python api_server.py`

### Docker

```dockerfile
FROM python:3.10-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

CMD ["python", "api_server.py"]
```

## 📈 Мониторинг работы

Проверьте логи:
```bash
tail -f app.log
```

Статус API:
```bash
curl https://ваш-сервер.com/health
```

## 🔒 Безопасность

- Никогда не коммитьте токены в git
- Используйте переменные окружения
- Настройте CORS правильно
- Ограничьте rate limiting

## 📝 Примеры использования

### Добавить конкурента через код

```python
monitor = InstagramMonitor()
monitor.add_competitor("nike")
monitor.add_competitor("adidas")
```

### Запустить разовую проверку

```python
import asyncio

monitor = InstagramMonitor(telegram_token="TOKEN")
asyncio.run(monitor.monitor_cycle(telegram_chat_id="YOUR_ID"))
```

### Получить статистику

```python
api = MonitorAPI(monitor)
alerts = api.get_alerts(limit=5)
print(alerts)
```

## 🐛 Решение проблем

**Проблема:** Нет уведомлений в Telegram
- Проверьте токен бота
- Убедитесь, что chat_id правильный
- Напишите боту хоть раз первым

**Проблема:** Не получает данные из Instagram
- Проверьте API ключи
- Убедитесь в наличии лимитов API
- Проверьте логи на ошибки

**Проблема:** Слишком много/мало алертов
- Настройте порог детекции
- Измените интервал проверки
- Скорректируйте критерии тренда

## 📞 Поддержка

Если нужна помощь:
1. Проверьте логи
2. Убедитесь, что все токены настроены
3. Протестируйте API endpoints отдельно

## 📄 Лицензия

MIT License - используйте свободно для личных и коммерческих проектов.

---

**Важно:** Уважайте лимиты API и Terms of Service платформ. Частые запросы могут привести к блокировке.
